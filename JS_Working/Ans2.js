//Calling the function before its declaration
console.log(multiplyNumber(2,3));

// declaring the function expression by assigning anonymous func. to a variable.
const multiplyNumber = function(num1,num2){
// defining the function
  return num1 * num2;
}